{{- if and .Values.crossplane_providerconfig.aws.enabled .Values.cloud.aws.services.apps.ingress.enabled -}}
apiVersion: helm.crossplane.io/v1beta1
kind: Release
metadata:
  name: {{ .Values.global.app.name }}-eks-ingress-nginx
  namespace: {{ .Release.Namespace }}
  labels:
    appName: {{ .Values.global.app.name }}
spec:
  forProvider:
    chart:
      name: ingress-nginx
      repository: https://kubernetes.github.io/ingress-nginx
      version: {{ .Values.cloud.aws.services.apps.ingress.version }}
    namespace: ingress-nginx
    values:
      controller:
        allowSnippetAnnotations: true
        service:
          annotations:
            service.beta.kubernetes.io/aws-load-balancer-type: nlb
            {{- if and .Values.cloud.aws.services.apps.ext_dns.annotate .Values.cloud.aws.services.apps.clappit_iam.hosts }} 
            external-dns.alpha.kubernetes.io/hostname: {{ join "," .Values.cloud.aws.services.apps.clappit_iam.hosts  | quote }}
            {{- end }}
  providerConfigRef:
    name: {{ .Values.global.app.name }}-eks-helm
{{- end }}